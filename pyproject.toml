[project]
name = "cafe_backend"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "OtavioAugustodaRoza", email = "otavioaugustodaroza@gmail.com"},
]
dependencies = ["django>=6.0.7"]
requires-python = ">=3.13"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
